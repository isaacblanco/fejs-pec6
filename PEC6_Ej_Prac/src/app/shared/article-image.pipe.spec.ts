import { ArticleImagePipe } from './article-image.pipe';

describe('ArticleImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleImagePipe();
    expect(pipe).toBeTruthy();
  });
});
